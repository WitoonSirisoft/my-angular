apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: angular
spec:
  template:
    spec:
      containers:
        - image: docker.io/witoonruamngoen/angular
          ports:
            - name: http1
              containerPort: 80
              